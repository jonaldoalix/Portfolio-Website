services:
  portfolio-website:
    container_name: portfolio-website
    build: .
    image: portfolio-website:local
    restart: unless-stopped
    ports:
      # fsb-03 Tailscale bind (see nathan docs/PORTS.md)
      - "100.64.0.7:8091:80"
